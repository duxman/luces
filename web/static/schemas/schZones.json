{
  "title" : "Defined Zones",
  "type" : "object",
  "id" : "Zones",
  "properties" :
  {
    "ZonePinType": {"type": "string", "description": "Zone Type", "enum": ["GPIO", "REMOTE"], "default": "GPIO"},
    "MQTT_TOKEN": {"type": "string", "description" :  "Mqtt token to send the messages"},
    "Zones"    : {
      "type"  : "array",
      "format": "tabs-top",
      "title" : "Zones Configuration",
      "items" : {
        "id" : "Zone",
        "title"     : "Zone",
        "type"      : "object",
        "properties": {
          "ZoneName": { "type": "string"},
          "ZoneId": { "type": "string"},
          "ZoneType": {"type": "string","enum": ["SPECTRUM","ALONE"],"default": "SPECTRUM", "description": "SPECTRUM = Normal Led Strip ALONE = Always on ligths" },
          "ZonePins": {"type": "string","description" : "GPIO PIN"   }
        }
      }
    }
  }
}